멘토님
ai-hub에 있는 데이터들이 전반적으로 모델을 fine-tuning하거나 scratch부터 만들 때, 사용하는 데이터인 경우가 많던데, 이를 현 프로젝트에서 어떻게 활용하면 좋을지

ai-hub 데이터 전처리 필요
카테고리에 정의별 질문 정의 = 답변또한 카테고리화 후
카테고리 5-10개의 카테고리, 시연시도 각 카테고리멸 5-10개의 질문으로

rule의 우선순위
rule에서 제대로 걸러지는지, 제대로된 보고서가 나오는지
rag에 답변이 없으면, 담당자(어느 분야)의 확인이 필요하다고 나오기

llm을 통해서 나오는 답변의 가이드라인 필요

9번 조건은 반드시 실행
답변은 15줄 정도

교수님 => 그냥 데이터 자체를 바꾸는 것이 좋을 것 같다.
1. 2개의 데이터 베이스를 사용한다. chroma와 supabase를 사용하는데, chroma db에는 사용자의 질문들이 들어가 있고, supabase는 상담사나 직원의 답변이 들어가있는 상태이다. 
사용자의 입력이 들어오면, 멕터화를 진행해서 데이터 chroma db에는 데이터 중에 가장 비슷한 질문을 찾아오고, 이 질문에 대한 답변을 supabase에서 가져와서 llm에게 질문 생성 프롬프트와 같이 주면서 rag를 구현한다. 이때, 이렇게 db에서 2개의 데이터 베이스 찾는 것을 구현하는 것이 좋을까?

2. 답변의 프롬프트에 rag의 내용을 얼마나 반영하는 것이 좋을까?

3. 카테고리별로 supabase랑 chroma를 각각 만들것인데, 이때 chroma db는 로컬에서 구현하고 있다. chroma db의 사이즈가 점점 더 커질 것인데, 그럼 cd를 할 때 점점 부담이 커질 것 같은데, 구글 버킷으로 올려두고 서버 통신으로 돌리는 것이 나을 지



출처가 좀 더 명확하게 보여야 한다. (쿠팡 자주 질문하는 질문 정확히 몇항이다)


1. 담당 부서로 정확하게 맵핑이 됐다.
2. 질문에 대한 명확한 분리가 필요하다.
3. 해당 질문이 왜 해당 부서로 할당이 됬는지 print문으로 llmops 활용하지 말고()
4. 관리자 페이지
5. 출처에 대한 세분화
6. 한국 전기 기술 협회
